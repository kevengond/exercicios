#include <stdio.h>

int main(void)
{
    // delcaração de variáveis
    int  n, i, prim, j, aux;

    // leitura do número
    scanf("%d", &n);
    // cálculo
   n = (33 * n) + (2 * n * 31);
    // saída
    printf("%d\n", n);
}